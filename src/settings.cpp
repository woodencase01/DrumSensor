#include "settings.h"

DrumSensor sensor[] =
{
  sensorPin0,
  sensorPin1,
  sensorPin2,
  sensorPin3,
  sensorPin4,
};

byte padType;
byte rimPad;
byte nbPadSensors;
byte centerThreshold;
byte padId;